# نظام التعرف على الوجوه - Face Recognition System

نظام ويب متكامل للتعرف على الوجوه مع تسجيل دخول بـ Google وإنشاء حساب.

## المميزات ✨

- ✅ تسجيل دخول بواسطة Google OAuth
- ✅ إنشاء حساب بكلمة مرور
- ✅ التقاط صور من الكاميرا
- ✅ إضافة وحفظ الوجوه مع الأسماء
- ✅ التعرف الفوري على الوجوه
- ✅ إخراج صوتي للأسماء المتعرف عليها
- ✅ واجهة عربية جميلة وعصرية
- ✅ قاعدة بيانات SQLite

## المتطلبات 📋

- Python 3.8+
- Webcam/Camera
- المكتبات في `requirements.txt`

## التثبيت 🚀

### 1. تثبيت المكتبات

```bash
cd web_face_recognition
pip install -r requirements.txt
```

### 2. إعداد Google OAuth (اختياري)

إذا أردت تفعيل تسجيل الدخول بـ Google:

1. اذهب إلى [Google Cloud Console](https://console.cloud.google.com/)
2. أنشئ مشروع جديد
3. فعّل Google+ API
4. أنشئ OAuth 2.0 credentials
5. أضف Authorized redirect URIs:
   - `http://localhost:5000/login/google/callback`
   - `http://127.0.0.1:5000/login/google/callback`
6. احصل على Client ID و Client Secret
7. افتح `app.py` واستبدل:
   ```python
   client_id='YOUR_GOOGLE_CLIENT_ID'
   client_secret='YOUR_GOOGLE_CLIENT_SECRET'
   ```

**ملاحظة:** إذا لم ترغب باستخدام Google OAuth، يمكنك استخدام خاصية إنشاء الحساب العادية فقط.

### 3. تشغيل التطبيق

```bash
python app.py
```

سيعمل الموقع على: `http://localhost:5000`

## طريقة الاستخدام 📖

### 1. التسجيل/تسجيل الدخول

- **إنشاء حساب جديد:**
  - اضغط "إنشاء حساب جديد"
  - أدخل الاسم والبريد وكلمة المرور
  - اضغط "إنشاء حساب"

- **تسجيل دخول بـ Google:**
  - اضغط "تسجيل الدخول بواسطة Google"
  - اختر حساب Google الخاص بك

- **تسجيل دخول عادي:**
  - أدخل البريد وكلمة المرور
  - اضغط "دخول"

### 2. إضافة وجه جديد

1. اضغط "📷 تشغيل الكاميرا"
2. اكتب اسم الشخص في الحقل
3. اضغط "📸 التقاط الصورة"
4. راجع الصورة الملتقطة
5. اضغط "✅ حفظ الوجه"

### 3. التعرف على الوجوه

1. تأكد من تشغيل الكاميرا
2. اضغط "🔍 التعرف على الوجه"
3. النظام سيتعرف تلقائياً على الوجوه كل ثانيتين
4. سيظهر الاسم على الشاشة: "أمامك أحمد"
5. سيتم نطق الاسم صوتياً

### 4. حذف وجه

- اضغط على "🗑️ حذف" في بطاقة الوجه المراد حذفه
- أكد الحذف

## بنية المشروع 📁

```
web_face_recognition/
├── app.py                      # التطبيق الرئيسي
├── requirements.txt            # المكتبات المطلوبة
├── templates/                  # صفحات HTML
│   ├── login.html             # صفحة تسجيل الدخول
│   ├── register.html          # صفحة إنشاء الحساب
│   └── dashboard.html         # الصفحة الرئيسية
├── static/                     # الملفات الثابتة
│   ├── css/
│   │   └── style.css          # ملف التصميم
│   └── js/
│       ├── main.js            # JavaScript للنماذج
│       └── dashboard.js       # JavaScript للوحة التحكم
├── uploads/                    # مجلد الصور المحفوظة
│   └── faces/                 # صور الوجوه
└── instance/                   # قاعدة البيانات
    └── face_recognition.db    # SQLite database
```

## قاعدة البيانات 💾

### جدول Users
- `id` - معرف المستخدم
- `email` - البريد الإلكتروني
- `password` - كلمة المرور (مشفرة)
- `name` - الاسم
- `google_id` - معرف Google (للمستخدمين عبر Google)
- `created_at` - تاريخ الإنشاء

### جدول Faces
- `id` - معرف الوجه
- `user_id` - معرف المستخدم (Foreign Key)
- `name` - اسم الشخص
- `image_path` - مسار الصورة
- `encoding` - تشفير الوجه (JSON)
- `created_at` - تاريخ الإضافة

## الأمان 🔒

- كلمات المرور مشفرة باستخدام `werkzeug.security`
- جلسات مؤمنة مع `session`
- التحقق من صحة المدخلات
- حماية ضد CSRF

## التقنيات المستخدمة 🛠️

- **Backend:** Flask (Python)
- **Database:** SQLite + SQLAlchemy
- **Authentication:** Google OAuth 2.0
- **Face Recognition:** face_recognition library (dlib)
- **Computer Vision:** OpenCV
- **Frontend:** HTML5, CSS3, JavaScript
- **Design:** Modern gradient design with animations

## المشاكل الشائعة 🔧

### مشكلة: الكاميرا لا تعمل
**الحل:** تأكد من إعطاء المتصفح صلاحية الوصول للكاميرا

### مشكلة: Google OAuth لا يعمل
**الحل:** 
1. تأكد من إعداد Google Cloud Console صحيح
2. تأكد من Client ID و Secret صحيحين
3. تأكد من Redirect URI مضاف

### مشكلة: التعرف على الوجوه بطيء
**الحل:** 
- قلل عدد الوجوه المحفوظة
- حسّن جودة الإضاءة
- استخدم CPU/GPU أفضل

### مشكلة: فشل في تثبيت face_recognition
**الحل:**
- Windows: قد تحتاج لتثبيت Visual C++ Build Tools
- Linux: قد تحتاج لتثبيت cmake و dlib
- macOS: استخدم `brew install cmake`

```bash
# Linux
sudo apt-get install cmake
sudo apt-get install python3-dev

# macOS
brew install cmake
```

## التطوير المستقبلي 🚀

- [ ] دعم تسجيل دخول بـ Facebook/Twitter
- [ ] التعرف على وجوه متعددة في نفس الوقت
- [ ] إحصائيات وتقارير
- [ ] تطبيق موبايل
- [ ] API للتكامل مع تطبيقات أخرى
- [ ] دعم لغات إضافية

## الترخيص 📄

هذا المشروع مفتوح المصدر للاستخدام التعليمي والشخصي.

## الدعم 💬

للأسئلة والمساعدة، يمكنك:
- فتح Issue في GitHub
- التواصل عبر البريد الإلكتروني

---

**تاريخ الإنشاء:** 2025-12-12
**الإصدار:** 1.0.0
**المطور:** AI Assistant

استمتع بالتعرف على الوجوه! 🎉
